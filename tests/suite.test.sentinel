import "Test_HCLass_Member"

tests = {
    "Member": Test_HCLass_Member.Run,
}

all_tests_valid = true

for tests as name, test {
    results = test()

    if results is false {
        all_tests_valid = false
    }
}

main = rule {
    all_tests_valid
}