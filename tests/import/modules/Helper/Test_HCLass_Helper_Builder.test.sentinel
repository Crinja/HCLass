import "types"

Test = func(functions) {
    if length(functions) < 1 {
        error("Functions given to Test Builder must be in a list.")
    }

    test_functions = functions

    return func() {
        results = []

        for test_functions as function {
            append(results, function())
        }

        return all results as r { r }
    }
}